services:
  db:
    image: postgres:16-alpine
    container_name: sigcpl_db
    environment:
      POSTGRES_USER: sigcpl
      POSTGRES_PASSWORD: sigcpl
      POSTGRES_DB: sigcpl
    ports:
      - "5433:5432"
    volumes:
      - sigcpl_db_data:/var/lib/postgresql/data

volumes:
  sigcpl_db_data:
